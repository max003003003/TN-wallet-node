const account = {
    updateAccountBalance: (account_id) => {
        throw new Error("update faild")
        // return model.sequelize.transaction((t) => {
        //     //update
        // })
    },
    updateAccountBalance2: (account_id) => {
       return account_id
        // return model.sequelize.transaction((t) => {
        //     //update
        // })
    }
}
module.exports = account